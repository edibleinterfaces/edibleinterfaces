const masterConfig = {
    pinned: [
        // list of appIds pinned to main page 
    ]
};

// one per app
const appConfig = {
    appId: 'NOTASK', 
    version: '1.0.0',
    urlPrefix: 'notask',  // notask.edibleinterfaces.com
};
